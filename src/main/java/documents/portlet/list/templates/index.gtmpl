<div class="container">
<% if (files!=null) { %>
  <ul>
  <%
     files.each { file -> %>
       <li>${file.name}

  <% } %>
  </ul>
<% } %>

		<div id="dropbox">
			<span class="message">Drop files here to upload. <br /><i>(they will only be visible to you)</i></span>
		</div>

</div>
